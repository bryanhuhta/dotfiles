#!/bin/bash
set -euo pipefail

managed='{
  "credsStore": "osxkeychain",
  "currentContext": "orbstack"
}'

current=$(cat)
[[ -z "$current" ]] && current='{}'

jq --argjson managed "$managed" '. * $managed' <<<"$current"
