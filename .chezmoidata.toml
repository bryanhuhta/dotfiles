# Repo-wide template data for the Linux profiles (tracked, unlike the
# machine-local chezmoi.toml).
#
# The nvm, node, and MarkEdit-preview pins moved to
# personal-mac/.chezmoidata.toml and work-mac/.chezmoidata.toml along with the
# profiles that use them. Nothing in this tree reads template data yet.
